<div class="col-md-12">
  <div class="group">
    <%= link_to("编辑电影", edit_movie_path(@movie), class: "btn btn-primary pull-right")%>
  </div>
  <h1><%= @movie.title %></h1>
  <p><%= @movie.description %></p>
</div>
